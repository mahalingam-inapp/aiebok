# Lab 03 — Basic RAG

The “generator” deliberately returns extracted evidence so the lab focuses on retrieval and citations. Add chunk IDs, metadata filters, a lexical baseline, retrieval tests, and then an optional model call behind a clearly separated function.
