# Lab 02 — Semantic Search

This toy uses feature hashing rather than learned embeddings, making every stage visible and dependency-free. Run `python main.py`, then follow the exercises in `docs/labs/semantic-search.md`.
