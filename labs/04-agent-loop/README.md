# Lab 04 — Agent Loop

This deterministic loop isolates the architecture from model behavior. Add a failing tool, retry budget, checkpoint serialization, approval before a destructive action, and tests that termination always occurs.
