# Fallback Degrade

## Context

Graceful degradation when models or retrieval fail.

## Solution

Define cheaper/safer fallback path with user messaging.

## Consequences

Availability during incidents. Reduced quality in fallback mode.

## Do not use when

Hard-fail acceptable for batch jobs.
