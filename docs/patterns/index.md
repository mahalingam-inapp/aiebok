# Pattern Library

Patterns capture recurring context, forces, solution shape, consequences, failure modes, and variants.

Starter patterns: [planner–executor](planner-executor.md) and [human approval](human-approval.md).

Expansion queue: router, fallback cascade, evaluator–optimizer, map–reduce, supervisor–worker, semantic cache, context compressor, retrieval fusion, citation validator, tool adapter, durable checkpoint, and circuit breaker.
